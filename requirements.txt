scikit-learn==1.5.2
python-fasthtml>=0.13.0   # earlier wheels (0.8 - 0.12) raise NameError 'Any' on import
matplotlib==3.9.2
scipy==1.14.1
numpy==2.1.2
pandas==2.2.3
pytest
flake8
ipython
# Install the bundled employee_events package so its sqlite-backed query
# helpers (QueryBase / Employee / Team) are importable from the report:
-e ./python-package
